# 2026-06-05 工作日志

## 任务：修复和完善 yigeworks.com，生成课程内容和营销文案

### 网站修复
- 克隆了 tdmy2011/github.io 仓库到本地工作区
- 创建了 `.nojekyll` 文件（防止 GitHub Pages 用 Jekyll 处理）
- 将 `github-pages-deploy/` 目录下的正确静态文件复制到根目录
- 检查了 JS 代码中的 classList 拼写（全部正确，无需修复）
- 更新了 index.html 导航栏，添加了到 `course.html` 和 `东南亚四国法规对照表.html` 的链接

### 课程内容生成（新增 3 个 HTML 文件）
1. `ISO14001_环境因素识别操作指南.html` - ISO 14001 环境因素识别四步法 + 重要性评价矩阵 + 四国环保法规对照
2. `双体系整合实施指南.html` - ISO 45001+ISO 14001 整合路线图 + 15份程序文件整合方案 + 12项自检清单
3. `东南亚四国法规对照表.html` - 越南/泰国/印尼/尼日利亚四国安全+环保法规逐条对照 + 风险提示

### 营销文案生成
- `双体系营销文案包.md` - 包含网站、YouTube、LinkedIn、微信、邮件营销（3封序列）、付费广告全平台文案

### 待用户完成
1. GitHub 认证配置后 push 本地 commits（2个未推送的 commit）
2. 在 GitHub repo Settings > Pages 中启用 GitHub Pages（选择 main 分支 + / (root) 目录）
3. 在腾讯云 DNS 中确认 yigeworks.com 的 CNAME 指向 tdmy2011.github.io
4. 替换 index.html 中的第三方工具 ID（GA4 / PayPal / Stripe / Mailchimp）

## 任务：Django 后端 + Geo 全球获客营销后台架构升级

### 技术选型与依赖
- Django 6.0 + Django REST Framework 3.17
- django-countries 8.2 (国家代码字段)
- Celery 5.6 + Redis 8.0 (异步任务队列)
- GeoIP2 5.2 (IP 地理定位)
- Gunicorn 26.0 (WSGI 生产服务器)
- WhiteNoise 6.12 (静态文件压缩)
- python-decouple 3.8 (环境变量管理)
- psycopg2-binary 2.9 (PostgreSQL 驱动)

### marketing app 完整实现
- models.py: Campaign (推广活动, 含 UTM + 自动化规则), VisitorLog (全球访客日志, GDPR 合规 IP 哈希), Lead (营销线索, 五阶段漏斗)
- admin.py: Django Admin 定制仪表盘 — Chart.js 实时访客趋势图 + 国家饼图 + 概览卡片; Campaign/VisitorLog/Lead 三个 ModelAdmin 完整注册
- tasks.py: process_visitor (异步入库), apply_auto_rules (自动营销规则引擎), send_welcome_email (延迟欢迎邮件), daily_geo_report (每日 Geo 报告)
- middleware.py: GeoVisitorMiddleware — HTTP 请求级 IP 解析 + 设备检测 + UTM 提取 + Celery 异步入库

### Django 配置 (settings.py)
- 自动 SQLite/PostgreSQL 双模式 (DEBUG=True 或 DJANGO_DEV=True 时切换)
- Redis/LocMem 缓存双模式
- HTTPS 强制重定向 + HSTS + Clickjack 防护
- Admin 路径可自定义 (非默认 /admin/)
- GDPR 隐私政策 URL 配置
- Celery Beat 每日 Geo 报告调度
- DRF Rate Throttle 限流
- 多语言支持 (en, zh-hans, vi, th)
- 生产级日志配置 (控制台 + 轮转文件)

### Docker Compose 六服务编排
- web (Django + Gunicorn, Port:8000)
- db (PostgreSQL 15, 健康检查)
- redis (Redis 7 Alpine, 持久化)
- celery-worker (异步任务处理, 4 workers)
- celery-beat (定时任务调度)
- nginx (反向代理 + SSL + 静态文件缓存 + Gzip)
- 共享卷: postgres_data, redis_data, static_volume, media_volume

### GitHub Actions CI/CD
- .github/workflows/deploy.yml: 双 Job 流程
  - test: PostgreSQL + Redis Service Containers → pip install → migrate → test → collectstatic
  - deploy: SSH to Lighthouse → git pull → migrate → collectstatic → docker compose restart → 健康检查

### Nginx 配置
- HTTP → HTTPS 301 强制跳转
- TLS 1.2/1.3 + ECDHE 加密套件
- 安全头: XFO, XSS, HSTS, Referrer-Policy
- /static/ 30天 CDN 缓存 + immutable
- /media/ 7天缓存
- Gzip 压缩 (text/css/js/json/html)
- 50MB 上传限制

### 当前状态
- Django 系统检查: ✅ 0 issues
- 数据库迁移: ✅ 4个模型表已创建 (Campaign, VisitorLog, Lead + Django 内置)
- Git: ✅ 已 commit 到本地 (HEAD → main, 领先 origin/main 4 commits)
- 待 push: 需要 GitHub PAT Token
